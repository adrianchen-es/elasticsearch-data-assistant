# Docker Bake environment variables
# Copy this to .env.bake and modify as needed

# Registry settings (empty for local builds)
REGISTRY=

# Image tag (default: latest)
TAG=latest

# Cache settings for local development
CACHE_FROM=type=local,src=/tmp/.buildx-cache
CACHE_TO=type=local,dest=/tmp/.buildx-cache-new,mode=max

# Alternative cache settings for CI/CD or registry-based caching
# CACHE_FROM=type=registry,ref=your-registry.com/cache:buildcache
# CACHE_TO=type=registry,ref=your-registry.com/cache:buildcache,mode=max

# Build arguments
PYTHON_VERSION=3.11
NODE_VERSION=20

# Platform targets (for multi-platform builds)
PLATFORMS=linux/amd64
